using System;
using System.Collections.Generic;

namespace Nekl_proj
{
    public class Logic
    {
        private double maxDeviationSpeedPerTick, maxHeightSpeedPerTick;
        
        private enum DeviationType {  } // TODO
        private enum HeightType { VeryClose, Close, Far }
        private enum SpeedType { Up, DownSlow, DownFast }

    }
}